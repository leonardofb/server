const http = require("http");

const host = 'localhost';
const port = 3000;
//re.metht -> Trae el metodo que se esta usando en la solicitud
// req.url -> Tra

const server = http.createServer(function (req, res) {});
server.listen(port, host, () => {
   
    //Esta es la deficnicion de una ruta
   if(a){


    //res.statusCode == 200;
    //res.end("Hasta luego");
//req.method ==='GET' $$ req.url ==='/user'

/*
adjuntar toda la logica, que permite retornar una lista de usauarios
*/ 

   }else if (a){
   
//req.method ==='POS' $$ req.url ==='/user'
   }

    //console.log(`Server is running on http://${host}:${port}`);


});

server.listen(port,()=>{
console.log.apply("SERVIDOR CORRIENDO");

})